# Dream journal — 2026-07-04

Second dream cycle. Bootstrapped the sliding-floor cursor (no `.floor` existed): mined the whole
WORKLOG this once, window **2026-06-26 .. 2026-07-04** (96 entries); persisted next floor
**2026-06-27** (clamped by the 7-day minimum — script output, verified via `cat .floor`).

## Memory
- All 6 prior memories live and tight — nothing archived or merged.
- **Fixed stale claim in `demo-url.md`:** it said the demo does NOT auto-deploy (manual Railway
  trigger required). Auto-deploy has been live since 2026-07-03 (Railway GitHub App, CI-gated;
  confirmed pass 95). Rewrote that paragraph; updated its MEMORY.md index line.
- **3 new memories:** `parallel-builders-need-isolation` (pass 95: second parallel builder
  clobbered the first's uncommitted WIP — worktrees or serial), `verify-effects-not-shapes`
  (passes 76–81: shape-green suite hid 4 broken features; never dismiss a flake without the
  mechanism), `engine-routes-need-engine-url-helpers` (pass 91: engine routes not resolvable via
  host url_for). Skipped `gem-never-instruments-host-apps` (already covered by `demo-posthog`) and
  the Dockerfile-builder pin (already noted in `puma-reads-web-concurrency-natively`).

## Decisions
- **Appended 2 back-fill entries to DECISIONS.md** (the ledger started 2026-07-02; the window
  surfaced earlier standing owner decisions never migrated): 2026-07-03 PostHog-on-demo directive
  (+ demo-only instrumentation scope rule), and 2026-06-26 cnc-cut / omakase-lint / sessions /
  GitHub-Pages ratification. Skipped one-time events (v0.2.0 version call, trusted-publisher
  registration) — not standing policy.
- **Decision-drift audit:** no `RETIRES:` tags exist in DECISIONS.md yet — nothing to grep. One
  informal drift found and flagged (below).

## Tool errors
- All recurring failures from the window are already resolved: `Agent type 'coder' not found`
  (~5 passes, fixed pass 62 by backporting the persona roster), bare `rake` Gem::LoadError
  (documented in AGENTS.md + memory), wrong-demo-host 404 false alarms (literal host now in the
  MEMORY.md index line). Nothing to patch this pass.

## Persona flags (flag-only → filed)
- Filed `consider-refresh-stale-status-lines-operator-persona-roadmap` (P3, verified via
  `tasks show`): operator.md's roadmap-order line still lists CHANGELOG → v0.1.2 → cnc (all long
  done), and CLAUDE.md still says the config DSLs "are being reworked" (shipped 12416c0).

## Ideas (seed: **[Cross-domain]** — day 185 % 16 = 9)
Grounding: this pass's delta (verify-effects lesson, auto-deploy now live, parallel-builder
hazard) × the seed's "apply a lesson from one domain to another." All cheap → `IDEAS.md` as
`proposed`, no owner proposals:
1. **Live-demo effect-level smoke check** — the verify-effects testing lesson applied to the
   marketing surface: the demo auto-deploys but nothing asserts it *works*.
2. **`isolation: worktree` for parallel builders** — ops fix for the pass-95 clobber.
3. **"See it live" README deep links** — growth follow-on from auto-deploy; measurable in PostHog.
