# Dream journal — 2026-06-30 (first cycle)

First dream cycle ever run (empty `docs/dreams/` before this). Pass 43, cold/reactive.

## Memory consolidation
- 3 memory files + index, all current and well-scoped — nothing archived or merged.
- Fixed stale ref in `demo-url.md`: it pointed the Railway-GitHub-App owner blocker at the retired
  `QUESTIONS.md`; repointed at the task board (`owner-one-time-dashboard-wiring-…`).

## WORKLOG mining (passes ~23–42)
- Most recent passes (35–42) are near-identical "board watch — fully gated" nothing-passes; durable
  lessons from them were already captured (demo bad-host probe, puma WEB_CONCURRENCY, grep both forms).
- **New lesson captured:** bare `rake` aborts with `Gem::LoadError` (system 13.3.1 vs Gemfile 13.4.2);
  `bundle exec rake` is the correct check-suite invocation (surfaced pass 39). Created memory
  `bundle-exec-rake.md` + index line, and documented it on the AGENTS.md check-suite line.

## Tool-error triage
- **Recurring:** bare `rake` → Gem::LoadError. Confirmed still reproducing this pass. Fixed safely by
  documenting `bundle exec rake` in AGENTS.md + a memory (above).
- **Resolved earlier:** demo health false-alarm from a reconstructed bad host — already fixed pass 38
  by putting the literal host in the MEMORY.md index line. No recurrence since.

## Persona hygiene (flag only)
- `conductor.md` (176 lines): no QUESTIONS.md refs (already cleaned), no obvious dead rules or
  contradictions. The cold/reactive cadence rules and the "file-don't-IC" routing guidance are
  consistent with observed behavior. No flags this cycle.

## Net
- Files touched: `AGENTS.md` (check-suite line), memory (`demo-url.md` fix, new `bundle-exec-rake.md`,
  `MEMORY.md` index). Journal added. No product code touched — correct for a gated maintenance pass.
