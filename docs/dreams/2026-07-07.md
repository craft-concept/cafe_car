# Dream journal — 2026-07-07

Fourth dream cycle. Sliding-floor window **[2026-06-28, 2026-07-07]** — previous pass mined through
2026-07-05 (Pass 96). New WORKLOG material this window: Passes 97–101, all captured/tactical.
Advanced floor 2026-06-28 → **2026-06-29** (old_floor+1 = 06-29 ≤ today−7d = 06-30, so no clamp).

## Memory
- All 10 memory files live, tight, and index-consistent. Nothing archived, merged, or shortened.
- MEMORY.md matches the 10 live files exactly — no drift to fix this pass.

## WORKLOG mining (Passes 97–101)
No new durable lessons — every pass in the window is either tactical execution of an
already-recorded decision or a hygiene/verification pass whose lesson is already a memory:
- **P97** shipped the positioning reframe (7/3 owner decision, already in DECISIONS.md); merged
  Dependabot bumps. Tactical.
- **P98** fixed the `belongs_to` column-sort bug — coder correctly used `left_outer_joins` over the
  task's suggested `references` (which builds no JOIN) and shipped an *effect-level* `sorted_test`.
  This is exactly the [[verify-effects-not-shapes]] pattern already captured; the join detail is a
  narrow Rails fact that belongs in git/WORKLOG, not a new operator memory.
- **P99–101** verification/hygiene: `/admin` "404" false alarm (same class as [[demo-url]]), closed
  the feature-gaps tracker after code-verifying every item, refreshed two stale status lines. No new
  lesson.

## Decisions
- No new standing owner decisions in the window. Passes 97–101 executed already-ledgered decisions
  (positioning reframe 7/3, PostHog demo-only 7/3); nothing to append to `DECISIONS.md`.
- Decision-drift audit: no `RETIRES:` tags in `DECISIONS.md` — nothing to grep.

## Tool errors
- None new in the window.

## Persona flags (flag-only) → escalated to a task this pass
- **Path drift, now flagged twice (7/05 + 7/07) → filed.** The dream persona
  (`.claude/agents/dream.md`, ~8 refs incl. `git add docs/DECISIONS.md`) and the `/dream` command
  both point at `docs/DECISIONS.md`, but the ledger is at repo-root `DECISIONS.md`
  (`git ls-files` confirms; AGENTS.md/CLAUDE.md use root). A future dream that literally appends to
  or `git add`s `docs/DECISIONS.md` would spawn an orphan diverging from the real ledger. Re-flagging
  a third time would be noise, so filed a P3 consider task
  (`consider-dream-tooling-references-docs-decisions-md-but-the-`) to *decide the direction* (fix
  tooling → root path, or move file → docs/) rather than edit the persona here — it's likely a
  fleet-template file (`bin/operate sync`), so a local edit could be overwritten and the fix belongs
  upstream.

## Ideas (seed: **[External]** — day 188 % 16 = 12: "fetch one recent market item")
Maintenance delta this pass was thin (only the re-flagged persona drift), so grounding leaned on the
seed. Ran one cheap web scan of the Rails view-layer market. Genuinely new, on-thesis signal:

- **The market is loudly relitigating the view primitive in 2026** — "ViewComponent vs Phlex vs
  Partials" is live content on RubyFlow/GoRails/DEV, with takes like "partials are the wrong answer
  for 2026." CafeCar's mechanism is partials/views (owner mandate: configure via views+partials, no
  DSLs), so the *narrative* is a perception headwind — but CafeCar's thesis is **orthogonal**: it's
  the convention layer *above* whatever view primitive you use (it lets you *delete* boilerplate view
  files; you keep VC/Phlex components only for what you customize). That's an un-addressed angle in
  our copy and a chance to ride existing traffic instead of competing with it.
- Routed **cheap** (a single positioning/launch post or README section through the voice gate, folded
  into the open `cafe_car-discoverability-launch` task; publishing live stays owner-in-the-loop per
  the positioning-art rule) → appended a `proposed` row to `IDEAS.md`. Operator runs it next GREEN
  pass. Not executed here.
- No other angle cleared the bar — existing IDEAS.md rows already cover smoke-check / worktree /
  deep-links / auto-code-review. Abstained on the rest.

Source: web scan of RubyFlow/GoRails/DEV/prodSens ViewComponent-vs-Phlex-vs-Partials coverage
(June 2026).
