# Dream journal — 2026-07-05

Third dream cycle. Sliding-floor window **[2026-06-28, 2026-07-05]** — the previous pass mined
through 2026-07-04, so the only new WORKLOG material was Pass 96 (PostHog follow-ups); its lesson
was already captured last pass as `per-task-commits-must-be-independently-green`. Advanced the
floor 2026-06-27 → **2026-06-28** (clamped by the 7-day minimum: today − 7d).

## Memory
- All 10 memory files live and tight — nothing archived, merged, or shortened.
- **Fixed index drift:** `per-task-commits-must-be-independently-green.md` existed on disk (added
  Pass 96) but was **missing from `MEMORY.md`**. Added its index line. The index now matches the
  10 live files.

## Decisions
- No new standing owner decisions in the window. Pass 96's owner asks (PostHog init spec, sessions
  fix, images fix, filing upstream #217) were tactical implementation directives, not standing
  policy — nothing to append to `DECISIONS.md`.
- Decision-drift audit: no `RETIRES:` tags in `DECISIONS.md` — nothing to grep.

## Tool errors
- None new. Pass 96 flag (4) noted demo-coder + turbo-coder had SendMessage disabled and relayed
  via board comments — a config quirk with a working fallback, not a recurring failure to patch.

## Persona flags (flag-only)
- **Path drift in the dream persona:** `.claude/agents/dream.md` step 2 references
  `docs/DECISIONS.md`, but the ledger actually lives at repo-root `DECISIONS.md` (confirmed via
  `git ls-files`; root path is also what CLAUDE.md/AGENTS.md use). Flagging only — did not edit
  the persona. (Last pass's `consider-refresh-stale-status-lines-operator-persona-roadmap` P3
  remains open; not re-filing.)

## Ideas (seed: **[External]** — day 187 % 16 = 11)
Fetched the Claude Code changelog (code.claude.com). Grounding: this pass's thin maintenance delta
(index fix) × the seed's external-signal lens. One genuinely new, on-thesis idea:
- **Auto `/code-review` on incoming community PRs** — `/code-review high --comment` (2.1.147+)
  posts inline PR findings; app-only `/install-github-app` (2.1.187) needs no workflow secrets.
  CafeCar's growth thesis is trust + PRs from strangers, so auto-reviewing external PRs is squarely
  on-mission. Needs an owner-only resource (GitHub App install / API-key secret) → **consequential**
  → filed a board **proposal** (`auto-code-review-on-incoming-community-prs`, assigned jeff) + an
  `IDEAS.md` `proposed` row. Not executed.
- No other new angle cleared the bar; abstained on the rest (existing IDEAS.md rows already cover
  the smoke-check / worktree / deep-link space).
