# Ideas — CafeCar

The venture's imagination stream: one line per idea, newest first. **Killed ideas stay listed** so
they aren't re-proposed.

**Status legend:** `proposed` (filed to the owner via `/propose`, awaiting a call) · `running`
(cheap idea in flight) · `kept` (tried, worked, kept) · `killed` (tried or proposed, then dropped —
say why).

How this works: see `AGENTS.md` → "Ideation". Cheap, reversible ideas you run yourself (log the
outcome here); consequential ones you `/propose` to the owner — they surface in holdco's
`bin/holdco asks --notify` digest under 💡 Proposals.

| Date | Idea | Status | Outcome / why |
|------|------|--------|---------------|
| 2026-07-07 | **Launch-post angle: enter the live "ViewComponent vs Phlex vs Partials" debate** — the Rails market is loudly relitigating the view primitive in 2026 (RubyFlow/GoRails/DEV posts, "partials are the wrong answer for 2026"). CafeCar's thesis is *orthogonal* to that fight: it's the convention layer **above** whatever view primitive you use — it lets you *delete* the boilerplate view files, and you keep components (ViewComponent/Phlex) only for the bits you customize. Ride that existing traffic instead of competing with it. Smallest version: one launch/positioning post — or a README "how CafeCar relates to ViewComponent/Phlex" section — through the voice gate, folded into the open `cafe_car-discoverability-launch` task. Know it worked via referral traffic in PostHog (project 496903). On-thesis; does NOT call CafeCar an admin framework. Cheap+reversible (drafting is cheap; publishing live stays owner-in-the-loop per the positioning-art rule). | running | Dream 2026-07-07, seed [External]. Pass 102: shipped the README "How CafeCar relates to ViewComponent & Phlex" section (0171d7c). Pass 106: refreshed the launch post (75dcdd3) with a "Pick your view primitive — CafeCar sits above it" hook riding the debate; emailed owner the preview for launch greenlight. Publishing live awaits owner venue/timing decision. |
| 2026-07-05 | **Auto `/code-review` on incoming community PRs** — CafeCar's growth thesis is trust + PRs from strangers (the README's own success metric); nothing currently reviews an external PR before a maintainer eyeballs it. Claude Code 2.1.147+ can post `/code-review high --comment` findings as inline PR comments, and 2.1.187 added app-only `/install-github-app` (no workflow secrets). Auto-reviewing each incoming PR signals a well-run project, speeds triage, gives contributors fast feedback, and catches bugs before they reach main (= potentially the next release). Consequential: needs an owner-only resource (GitHub App install / Anthropic API-key Actions secret), so proposed not run. | proposed | Dream 2026-07-05, seed [External] — filed to owner via board proposal |
| 2026-07-04 | **Live-demo effect-level smoke check** — the demo auto-deploys on every merge but nothing verifies it *works*, only that it renders; the pass-77 lesson (shape-green hid 4 broken features) applies to our top marketing surface too. Smallest version: a post-deploy script (CI job or scheduled check) that drives the live demo's core loops — create, edit, bulk action, dashboard — and asserts the effect persisted. Know it worked when a broken advertised feature turns something red instead of sitting silently broken in front of prospects. | proposed | Dream 2026-07-04, seed [Cross-domain] |
| 2026-07-04 | **`isolation: worktree` for parallel builders** — pass 95's second builder clobbered the first's uncommitted WIP (shared tree). Smallest test: next pass that dispatches 2+ builders passes `isolation: "worktree"` on each Agent call. Know it worked when parallel passes stop producing clobber/race incidents. | proposed | Dream 2026-07-04, grounded in new `parallel-builders-need-isolation` memory |
| 2026-07-04 | **"See it live" deep links in the README** — now that main auto-deploys to the demo, each README feature section can link straight to the page demonstrating it (charts tab, dashboard, bulk actions). Smallest version: 4–6 links added in one copy pass through the voice gate. Know it worked via PostHog referrer/pageview data on the demo (project 496903). | kept | Ran Pass 103 (2026-07-07): 5 "See it live →" links shipped (`014c8d6`), all targets re-curled 200, CI green. Watch PostHog demo referrers for click-through. |
| 2026-07-03 | **Dashboards + bulk actions via views/partials** (drop the config DSLs) — a design proposal grounding both features in CafeCar's existing view-override convention: a dashboard is one host-authored `cafe_car/dashboard/show` template composing `metric`/`chart` helpers; a bulk action decomposes into a `_bulk_actions` partial button + a model bang method + a policy predicate. Reworks the mechanism, keeps the features. | kept | Owner approved 7/3 19:32 ("very close!") with corrections: policy is the source of truth (`permitted_bulk_actions`/`permitted_metrics`, partials loop the list by default), button styles + all copy in locales. Both reworks SHIPPED — commit 12416c0, rake green, DSLs deleted. |
