= form_for object do |f|
  = card.section do
    = render "fields", f:

  = card.section class: "row" do
    = render("extra_fields", f:)

  = card.foot do
    = render "submit", f:
    = render("errors", f:)
