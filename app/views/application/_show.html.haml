- p = ui(record)
= ui.card do |card|
  = card.head do
    = p.try(:logo, width: 100)
    = card.title(tag: :h2) { p.title }

  = card.section do
    = p.attributes(except: %i{title updated_at created_at})

  = card.section do
    = p.associations
