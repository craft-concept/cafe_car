- title = breadcrumbs(link(model).index, title(t(:new, scope: %i[helpers title], model: model_name.human)))

= Page :slim, title: do |page|
  = page.Body do
    = render "alerts"
    = Card do |card|
      = render "form", card:
    = render "debug" if debug?
