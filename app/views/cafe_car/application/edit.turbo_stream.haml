- if action.edit?
  = render "popup" do
    = Card title: "Edit #{model_name.human}", actions: Close("✕") do |card|
      = render "form", card:
- else
  = turbo_stream.replace(dom_id(object, :edit)) do
    = render "form", card: ui.Card
