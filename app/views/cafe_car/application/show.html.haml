- title   = breadcrumbs(link(object).index, title(object))
- summary = ui(object).attributes :updated_at, :created_at, try: true
- body    = render("show")

= ui.page :full, title: do |page|
  = page.aside do
    = yield :left

  = page.body do
    = render "alerts"

    - if tab = params[:tab]
      -# - title tab.humanize
      -# = row bottom: true do
      -#   = page.head
      -#     %h1 object.try(:name).fmap {|name| "#{name}'s #{tab.humanize(capitalize: false)}" }.felse { tab.humanize }
      -#   = link "← Back to #{model_name.human}", link: path_super(object)
      -#
      -# = render "tab_#{tab}"
    - else
      = body
    = render "debug" if debug?

  = page.aside do
    = ui.card :sticky do |card|
      = card.section ui(object).controls
      = card.section summary

    = ui.card title: "Actions" do |card|
      = render("actions", card:)

    = ui.card title: "Notes" do |card|
      = render("notes", card:)

    = yield :right
